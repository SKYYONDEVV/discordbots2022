const discord = require('discord.js');

module.exports.run = async (client, message, args) => {

    message.channel.send("*Deze Bot is gemaakt door: $!𝗦𝗞𝗬𝗬!$#4918*")

}

module.exports.help = {
    name: 'created'
}